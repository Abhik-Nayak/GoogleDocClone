export  const formulaForColumnOfFour = (boadSize: number) =>
    boadSize * boadSize - (boadSize + boadSize + boadSize) - 1;

export  const formulaForColumnOfThree = (boadSize: number) =>
    boadSize * boadSize - (boadSize + boadSize) - 1;
